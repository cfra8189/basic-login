<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>User App</title>
    <link rel="stylesheet" href="/css/app.css" />
  </head>
  <body>
    <div class="app-header">
      <div class="app-logo"><img src="/ltl-logo.PNG" alt="logo"/></div>
      <h2>Edit User</h2>
      <div>
        <label class="theme-switch">
          <input id="themeToggle" type="checkbox">
          <span class="slider"></span>
        </label>
      </div>
    </div>

    <div class="app-container">
      <h1>Edit User</h1>
      <form action="/users/<%= user._id %>?_method=PUT" method="POST">
        <!--  NOTE: the form is pre-populated with values for the server-->
        <label>Username <input type="text" name="username" value="<%= user.username %>" /></label>
        <label>Email <input type="text" name="email" value="<%= user.email %>" /></label>
        <label>Password <input type="password" name="password" value="" /></label>
        <label>Required <input type="checkbox" name="required" <% if (user.required === true) { %> checked <% } %> /></label>
        <div style="margin-top:12px">
          <button class="btn" type="submit">Update User</button>
          <a href="/users" style="margin-left:12px;">Back to Users List</a>
        </div>
      </form>
    </div>
    <script src="/js/theme.js"></script>
  </body>
</html>
